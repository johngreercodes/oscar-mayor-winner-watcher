---
---

<!-- {movies[2025].map((movie)=>{
    <div class="movie-card">
        <figure>
            <img src={data.imageSet.verticalPoster.w720}/>
        </figure>
        <div class="movie-info">
            <h1>{data.title}</h1>
            <p>{data.directors}</p>
            <p>{convertRuntime(data.runtime)}</p>
            <div class="streaming-container">
                <button type="button">{data.streamingOptions.us[0].service.name}</button>
            </div>
        </div>
    </div>
})} -->

<style>
    .movie-card {
        color: white;
    	background: #353535;
        max-width: 300px;
        margin: auto;
        padding: 5px;
        display: grid;
        grid-template-columns: 2fr 1fr;
    }
    .movie-info {
        display: flex;
        flex-direction: column;
    }
    img {
        max-width: 100%;
        max-height: 100%;
    }
</style>